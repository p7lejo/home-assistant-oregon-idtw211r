name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Determine integration version
        id: version
        run: |
          VERSION=$(jq -r '.version' custom_components/oregon_idtw21r/manifest.json)
          if [ -z "$VERSION" ] || [ "$VERSION" = "null" ]; then
            echo "Could not determine integration version."
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create release if missing
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v${{ steps.version.outputs.version }}"
          if gh release view "$TAG" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            echo "Release $TAG already exists."
          else
            gh release create "$TAG" \
              --repo "${{ github.repository }}" \
              --target "${{ github.sha }}" \
              --title "$TAG" \
              --notes "## Oregon Scientific IDTW21xR $TAG

### Fixed
- Correctly handle the Oregon Scientific no-value temperature sentinel 0x7FFF.
- Outdoor temperature 2 now reports unavailable instead of 3276.7 °C when the corresponding sensor is not available.
- Apply the same handling to Outdoor 3 and temperature min/max values.

### Technical
- Native Home Assistant Bluetooth/Bleak GATT communication remains unchanged.
- No additional Python or system dependencies are required."
          fi
